class AppRoutes {
  AppRoutes._();

  static const root = "/";

  // Auth routes
  static const auth = "/auth";
  static const register = "/register";
  static const login = "/login";

  // User routes
  static const userHome = "/userHome";
  static const slotListScreen = '/slotList';

  // Admin routes
  static const adminHome = "/adminHome";
}
